// Exemplo de uso do operador ||
let idade = 25;
if (idade < 18 || idade > 65) {
    console.log("Você é elegível para o desconto de idoso.");
} else {
    console.log("Você não é elegível para o desconto de idoso.");
}
